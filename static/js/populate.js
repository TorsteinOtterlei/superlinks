
/* window.onload = function() {
    let html = "";
    const links = data.map((link, index) => {
        html =
        '<a href="' + link.link + '">' +
            '<div class="card-body">' + 
                link.name + 
            '</div>' +
        '</a>'
        ;
        document.getElementById("link"+index).innerHTML = html;
    });
} */